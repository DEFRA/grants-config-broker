Experimenting with best way to do this still...

For now, create a new file called release, based on release.example.yml.

This should contain the grant you want to release and where to. For now this will be a single grant at a time.
